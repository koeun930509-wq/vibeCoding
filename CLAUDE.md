# CLAUDE.md

> **이 프로젝트는 코드를 작성하지 않는다.** 모든 동작은 GitHub MCP, Notion MCP, Resend MCP 도구 호출과 프롬프트 지시만으로 수행한다. 스크립트, 애플리케이션, 별도 실행 파일을 만들지 않는다. 최종 산출물은 재사용 가능한 슬래시 명령 `/sync-repos` 하나다.

## 프로젝트 개요

GitHub 계정의 리포지토리를 Notion 데이터베이스(`GitHub 리포지토리 관리`, VibeCoding 페이지 하위)로 동기화하고, 저장소별 README/구조를 분석해 요약 리포트를 Notion 페이지 본문에 작성하며, 활동상태를 자동 분류하고 포트폴리오 노출 저장소를 자동 선정한 뒤, 완료 알림을 이메일로 발송하는 워크플로우. 전체 요구사항은 [prd.md](./prd.md) 참고.

이 Notion DB를 CMS로 사용하는 포트폴리오 웹앱 관련 규칙은 [CLAUDE-webapp.md](./CLAUDE-webapp.md)에 별도로 정리되어 있다.

## 규칙

아래 규칙은 `/sync-repos` 실행 시 반드시 그대로 지켜야 한다.

1. **[스키마]** Notion 데이터베이스 `GitHub 리포지토리 관리`의 속성 10개와 타입은 다음과 같다.

   | # | 속성명 | 타입 |
   |---|---|---|
   | 1 | 저장소명 | title |
   | 2 | URL | url |
   | 3 | 생성일 | date |
   | 4 | 설명 | rich_text |
   | 5 | 분석상태 | select (대기 / 완료 / 실패) |
   | 6 | 마지막푸시일 | date |
   | 7 | 기술태그 | multi_select |
   | 8 | 활동상태 | select (방치됨 / 활발함) |
   | 9 | 포트폴리오 노출 | checkbox |
   | 10 | 커버이미지 | files |

2. **[리포트 형식]** 저장소별 Notion 페이지 본문은 다음 4개 섹션을 이 순서 그대로 포함한다: `[기술스택]` / `[추정 핵심기능]` / `[구조 관찰]` / `[구조 다이어그램]`

3. **[활동상태]** 마지막푸시일이 오늘 기준 6개월 이상 지났으면 `방치됨`, 아니면 `활발함`으로 분류한다.

4. **[포트폴리오 노출]** 마지막푸시일 최신순으로 정렬해 상위 5개만 체크(true), 나머지는 모두 해제(false)한다.

5. **[커버이미지]** `https://opengraph.githubassets.com/1/소유자/저장소명` 형식의 URL을 외부 파일로 등록한다.

6. **[배치]** 저장소가 10개를 넘으면 10개씩 나눠서 처리하고, 묶음마다 진행 상황을 보고한다.

7. **[중복 방지]** 등록·삽입 전에 저장소명으로 먼저 조회해서 이미 있으면 건너뛴다.

8. **[다이어그램]** Mermaid flowchart(`graph TD`)로 작성하고, 모든 노드 라벨은 큰따옴표로 감싼다. Notion 코드 블록의 언어는 `mermaid`로 지정한다.

9. **[근거 규칙]** 근거가 없으면 `정보 부족`이라고 쓰고 절대 추측하지 않는다.

10. **[에러 처리]** Notion API가 429(요청 과다)를 반환하면 몇 초 기다렸다가 같은 묶음을 재시도한다. 3회 연속 실패하면 해당 저장소의 분석상태를 `실패`로 남기고 다음으로 넘어간다.

11. **[보고 형식]** 마지막에 전체 / 신규 등록 / 분석 완료 / 건너뜀 / 실패 개수를 표로 정리해서 보고한다.
