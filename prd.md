# PRD: `/sync-repos` — GitHub 리포지토리 → Notion 포트폴리오 자동 동기화

- 문서 버전: v1.0
- 작성일: 2026-08-05
- 대상 워크스페이스: Notion "VibeCoding" 페이지 하위 `GitHub 리포지토리 관리` 데이터베이스
- 산출물 형태: 재사용 가능한 슬래시 명령 `/sync-repos` (코드 작성 없음, MCP 도구 호출 + 프롬프트로만 동작)

---

## 1. 배경 및 목적

사용자는 GitHub 계정에 흩어져 있는 리포지토리들을 한 곳에서 파악하고, 포트폴리오로 활용할 수 있는 저장소를 자동으로 골라내고 싶어 한다. 매번 수동으로 저장소를 열어보고 README를 읽고 Notion에 정리하는 대신, `/sync-repos` 한 번 실행으로:

1. GitHub의 모든 리포지토리를 Notion DB에 최신 상태로 반영하고,
2. 각 저장소를 분석해 요약 리포트를 Notion 페이지 본문에 채우고,
3. 활동 여부를 자동 분류하고,
4. 포트폴리오에 노출할 저장소를 자동 선정하고,
5. 데이터베이스에 시각화 뷰(차트, 갤러리)를 구성하고,
6. 완료 시 이메일로 알림을 받는다.

## 2. 범위 (Goals)

- GitHub MCP로 사용자 계정의 리포지토리 목록·메타데이터·README·디렉토리 트리를 조회한다.
- Notion MCP로 기존 `GitHub 리포지토리 관리` 데이터베이스([참고 스키마](#5-notion-데이터베이스-스키마-기존))에 저장소별 페이지를 생성/갱신한다.
- 저장소별 Notion 페이지 본문에 다음 4개 섹션을 포함한 분석 리포트를 작성한다: 기술스택, 추정 핵심기능, 구조 관찰, 구조 다이어그램(mermaid 등 텍스트 다이어그램).
- 활동상태(방치됨/활발함)를 정해진 기준으로 자동 산정한다.
- 전체 저장소 중 "최근 활동한" 상위 5개를 포트폴리오 노출 = 체크로 표시한다(그 외는 해제).
- Notion 데이터베이스에 다음 뷰를 구성/갱신한다:
  - 기술스택 분포를 보여주는 차트 뷰
  - 활동상태 비율을 보여주는 차트 뷰
  - 포트폴리오 노출 저장소만 보여주는 갤러리 뷰
- 모든 작업 완료 후 Resend MCP로 사용자 이메일(koeun930509@gmail.com)에 완료 알림을 발송한다.
- 이 전체 흐름을 `/sync-repos` 슬래시 명령 하나로 반복 실행 가능하게 만든다(멱등적 재실행 지원).

## 3. 범위 밖 (Non-goals)

- 별도의 애플리케이션/스크립트/서버 코드를 작성하지 않는다. 오직 MCP 도구 호출 + 프롬프트 지시로만 동작한다.
- CI/CD, 웹훅 기반 실시간 동기화는 다루지 않는다(수동/명시적 실행 기준).
- 비공개(private) 조직 리포지토리 전체를 포함할지, 사용자 개인 계정 리포지토리만 포함할지는 실행 시 옵션으로 명시하되, 기본값은 "사용자 소유 리포지토리 전체(포크 제외)"로 한다.
- README가 없는 저장소에 대한 상세 분석은 "정보 부족"으로 명시하고 실패 처리하지 않는다.

## 4. 사용자 시나리오

```
사용자: /sync-repos
  → GitHub 리포지토리 목록 조회
  → 각 저장소: README/구조 분석 → 요약 생성 → Notion 페이지 upsert
  → 활동상태 계산 → 최근 활동 Top 5 선정 → 포트폴리오 노출 갱신
  → 차트뷰 2개 + 갤러리뷰 1개 구성/갱신
  → Resend로 완료 메일 발송 (처리 건수, 신규/갱신/실패 요약 포함)
  → 콘솔에 실행 요약 출력
```

재실행 시나리오: 이미 존재하는 저장소 페이지는 속성/본문을 덮어쓰기 갱신(upsert)하며, 신규 저장소만 페이지를 생성한다. 삭제된 GitHub 저장소는 Notion에서 "분석상태 = 실패" 또는 별도 표시로만 남기고 자동 삭제하지는 않는다(데이터 보존 우선).

## 5. Notion 데이터베이스 스키마 (기존)

이미 생성 완료된 데이터베이스: `GitHub 리포지토리 관리` (VibeCoding 페이지 하위)

| 속성명 | 타입 | 비고 |
|---|---|---|
| 저장소명 | title | GitHub repo name |
| URL | url | 저장소 URL |
| 생성일 | date | GitHub 저장소 생성일 |
| 설명 | rich_text | GitHub description |
| 분석상태 | select (대기/완료/실패) | 이번 작업의 처리 상태 |
| 마지막푸시일 | date | 최근 커밋/푸시 일시 |
| 기술태그 | multi_select | 감지된 언어/프레임워크 |
| 활동상태 | select (방치됨/활발함) | §7 기준으로 자동 산정 |
| 포트폴리오 노출 | checkbox | §8 기준 상위 5개만 true |
| 커버이미지 | files | (선택) 대표 이미지 — 자동 채움 대상 아님, 수동 큐레이션 여지 |
| 버셀 배포 URL | url | (선택) Vercel 배포 주소 — 감지 가능 시 자동 채움, 아니면 공란 |

## 6. 기능 요구사항

### 6.1 저장소 수집 및 DB 동기화
- GitHub MCP로 사용자 계정의 리포지토리 목록을 페이지네이션 없이 전량 조회한다(포크 제외, 기본값).
- 저장소별로 `저장소명`으로 기존 Notion 페이지 존재 여부를 조회해 upsert(있으면 갱신, 없으면 생성) 한다.
- 채우는 속성: URL, 생성일, 설명, 마지막푸시일, 기술태그(언어 통계 기반).

### 6.2 README/구조 분석 및 리포트 생성
각 저장소에 대해 GitHub MCP로 README 원문과 최상위~2단계 디렉토리 트리를 조회한 뒤, 프롬프트로 다음 4개 섹션을 생성해 Notion 페이지 본문에 작성한다:

1. **기술스택**: README + 디렉토리 트리 + 언어 통계 기반 추정 (예: 프레임워크, 주요 라이브러리, 런타임)
2. **추정 핵심기능**: README 설명/스크린샷 언급 등에서 유추한 핵심 기능 bullet list
3. **구조 관찰**: 폴더 구조에서 파악되는 아키텍처 패턴, 특이사항, 정돈 상태에 대한 짧은 서술
4. **구조 다이어그램**: 디렉토리 트리를 기반으로 한 텍스트 다이어그램(mermaid 코드 블록 또는 트리 형태)

README가 없거나 내용이 빈약한 저장소는 "분석 정보 부족"을 명시하고 가능한 범위(디렉토리 구조, 언어 통계)로만 리포트를 작성한다. 이 경우도 실패로 처리하지 않고 분석상태는 완료로 둔다(단, 리포트 상단에 "정보 제한적" 표기).

### 6.3 활동상태 자동 분류
- 기준: **마지막 푸시일 기준 90일** 이내면 `활발함`, 그 이상이면 `방치됨`.
  - (90일은 기본값 제안이며, 실행 시 사용자가 재정의 가능하도록 슬래시 명령 인자로 노출)

### 6.4 포트폴리오 노출 자동 선정
- 전체 저장소를 `마지막푸시일` 내림차순으로 정렬해 상위 5개만 `포트폴리오 노출 = true`, 나머지는 `false`로 설정(매 실행마다 재계산·덮어쓰기).
- 동률(같은 날짜) 처리 시 스타(star) 수 → 없으면 저장소명 알파벳 순으로 tie-break.

### 6.5 Notion 뷰 구성
데이터베이스에 아래 3개 뷰를 생성(이미 존재하면 재사용/갱신):
1. **기술스택 분포 차트** — 기술태그(multi_select) 기준 분포를 보여주는 차트 뷰
2. **활동상태 비율 차트** — 활동상태(select) 기준 비율을 보여주는 차트 뷰
3. **포트폴리오 갤러리** — `포트폴리오 노출 = true` 필터가 적용된 갤러리 뷰(커버이미지를 카드 이미지로 사용)

### 6.6 완료 알림
- Resend MCP로 사용자 이메일(koeun930509@gmail.com)에 실행 결과 요약 메일을 발송한다.
- 메일 본문 포함 항목: 총 처리 저장소 수, 신규 생성 수, 갱신 수, 실패 수(있다면 목록), 활발함/방치됨 개수, 이번에 선정된 포트폴리오 노출 5개 저장소명, Notion DB 링크.

## 7. 슬래시 명령 인터페이스

파일 위치(예정): `.claude/commands/sync-repos.md`

```
/sync-repos [--activity-days=90] [--top=5] [--include-forks]
```

- `--activity-days`: 활동상태 판정 기준 일수 (기본 90)
- `--top`: 포트폴리오 노출 선정 개수 (기본 5)
- `--include-forks`: 포크 저장소도 포함 (기본 미포함)

명령은 내부적으로 GitHub MCP → Notion MCP → Resend MCP 순으로 도구를 호출하는 절차를 프롬프트 지시문으로 기술하며, 별도 실행 파일/스크립트는 두지 않는다.

## 8. 비기능 요구사항

- **재사용성**: 슬래시 명령으로 언제든 재실행 가능해야 하며, 재실행 결과는 멱등적이어야 한다(중복 페이지 생성 금지).
- **부분 실패 허용**: 특정 저장소 분석 실패 시 전체 작업을 중단하지 않고 `분석상태 = 실패`로 표시 후 계속 진행, 최종 메일에 실패 목록 포함.
- **속도/제한**: GitHub API rate limit을 고려해 저장소 수가 많을 경우 순차 처리하되 진행 상황을 사용자에게 중간 보고한다.
- **투명성**: 각 단계(수집/분석/분류/뷰 구성/메일 발송) 완료 시 짧은 진행 메시지를 출력한다.

## 9. 전제조건 및 의존성

- GitHub MCP, Notion MCP 연결 완료(확인됨).
- **Resend MCP는 현재 미연결 상태** — `/sync-repos` 최초 실행 전 반드시 연결 및 발신 도메인/이메일 인증 설정이 필요하다. 이 PRD 승인 이후 별도 작업으로 연결한다.
- Notion `GitHub 리포지토리 관리` 데이터베이스 스키마는 기존 것을 그대로 사용(§5), 스키마 변경이 필요하면 별도 합의 후 진행.

## 10. 성공 기준 (Acceptance Criteria)

- [ ] `/sync-repos` 실행 시 GitHub 계정의 모든 non-fork 저장소가 Notion DB에 1:1로 존재한다.
- [ ] 각 저장소 페이지 본문에 4개 섹션(기술스택/추정 핵심기능/구조 관찰/구조 다이어그램)이 채워져 있다.
- [ ] 마지막푸시일 기준 90일 이내 저장소는 활동상태=활발함, 아니면 방치됨으로 정확히 표시된다.
- [ ] 마지막푸시일 기준 상위 5개 저장소만 포트폴리오 노출=true이다.
- [ ] 데이터베이스에 기술스택 분포 차트, 활동상태 비율 차트, 포트폴리오 갤러리 뷰가 존재한다.
- [ ] 실행 완료 후 사용자 이메일로 요약 메일이 수신된다.
- [ ] 동일 명령을 재실행해도 저장소 페이지가 중복 생성되지 않는다.

## 11. 오픈 이슈

1. Resend MCP 연결 필요 — 발신 도메인/API 키 설정 확인 필요.
2. "커버이미지" 자동 채움 여부 — 이번 범위에서는 자동 채우지 않고 수동 큐레이션 여지로 남겨둠(§5). 필요 시 README 내 이미지/OG 이미지 자동 추출로 확장 가능.
3. 비공개(private) 저장소 포함 여부 및 조직(org) 소유 저장소 포함 여부 확인 필요(기본값: 사용자 개인 소유만).
4. "버셀 배포 URL" 자동 감지 방법 — README/Vercel 배포 뱃지, `vercel.json` 존재 여부 등으로 추정하되 정확도 보장 불가, 감지 실패 시 공란 유지.
